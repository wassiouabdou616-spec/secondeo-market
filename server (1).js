const express=require("express");
const path=require("path");
const jwt=require("jsonwebtoken");
const bcrypt=require("bcryptjs");
const {Pool}=require("pg");
const Stripe=require("stripe");
require("dotenv").config?.();
const app=express();
const pool=new Pool({connectionString:process.env.DATABASE_URL});
const stripe=process.env.STRIPE_SECRET_KEY?new Stripe(process.env.STRIPE_SECRET_KEY):null;
app.use(express.json({limit:"2mb"}));
app.use(express.static(__dirname));
const sign=u=>jwt.sign({id:u.id,role:u.role},process.env.JWT_SECRET||"dev-only-secret",{expiresIn:"7d"});
async function auth(req,res,next){try{let h=req.headers.authorization||"";if(!h.startsWith("Bearer "))return res.status(401).json({error:"Connexion requise"});req.user=jwt.verify(h.slice(7),process.env.JWT_SECRET||"dev-only-secret");next()}catch(e){res.status(401).json({error:"Session invalide"})}}
function admin(req,res,next){if(req.user.role!=="admin")return res.status(403).json({error:"Administrateur requis"});next()}
app.post("/api/register",async(req,res)=>{try{let{name,email,password,role="buyer"}=req.body;if(!name||!email||!password)return res.status(400).json({error:"Champs requis"});let hash=await bcrypt.hash(password,12);let r=await pool.query("INSERT INTO users(name,email,password_hash,role) VALUES($1,$2,$3,$4) RETURNING id,name,email,role",[name,email.toLowerCase(),hash,role==="seller"?"seller":"buyer"]);res.status(201).json({user:r.rows[0],token:sign(r.rows[0])})}catch(e){res.status(400).json({error:"Email déjà utilisé ou données invalides"})}});
app.post("/api/login",async(req,res)=>{let r=await pool.query("SELECT * FROM users WHERE email=$1",[String(req.body.email||"").toLowerCase()]);let u=r.rows[0];if(!u||!(await bcrypt.compare(req.body.password||"",u.password_hash)))return res.status(401).json({error:"Identifiants incorrects"});res.json({token:sign(u),user:{id:u.id,name:u.name,email:u.email,role:u.role}})});
app.get("/api/products",async(req,res)=>{let q=String(req.query.q||"").trim(),cat=String(req.query.category||"").trim(),min=Number(req.query.min||0),max=Number(req.query.max||99999999);let r=await pool.query("SELECT p.*,u.name seller FROM products p JOIN users u ON u.id=p.seller_id WHERE p.status='approved' AND p.title ILIKE $1 AND ($2='' OR p.category=$2) AND p.price_cents BETWEEN $3 AND $4 ORDER BY p.created_at DESC",[`%${q}%`,cat,min*100,max*100]);res.json(r.rows)});
app.post("/api/products",auth,async(req,res)=>{if(!["seller","admin"].includes(req.user.role))return res.status(403).json({error:"Compte vendeur requis"});let b=req.body,r=await pool.query("INSERT INTO products(seller_id,title,description,price_cents,category,condition,city,status) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *",[req.user.id,b.title,b.description||"",Math.round(Number(b.price)*100),b.category||"Autre",b.condition||"Bon état",b.city||"",req.user.role==="admin"?"approved":"pending"]);res.status(201).json(r.rows[0])});
app.get("/api/cart",auth,async(req,res)=>{let r=await pool.query("SELECT c.product_id,c.quantity,p.title,p.price_cents,p.seller_id FROM carts c JOIN products p ON p.id=c.product_id WHERE c.buyer_id=$1",[req.user.id]);res.json(r.rows)});
app.post("/api/cart",auth,async(req,res)=>{await pool.query("INSERT INTO carts(buyer_id,product_id,quantity) VALUES($1,$2,1) ON CONFLICT(buyer_id,product_id) DO UPDATE SET quantity=carts.quantity+1",[req.user.id,req.body.productId]);res.status(201).json({ok:true})});
app.delete("/api/cart/:id",auth,async(req,res)=>{await pool.query("DELETE FROM carts WHERE buyer_id=$1 AND product_id=$2",[req.user.id,req.params.id]);res.json({ok:true})});
app.post("/api/checkout",auth,async(req,res)=>{let c=await pool.query("SELECT c.product_id,c.quantity,p.title,p.price_cents,p.seller_id FROM carts c JOIN products p ON p.id=c.product_id WHERE c.buyer_id=$1",[req.user.id]);if(!c.rows.length)return res.status(400).json({error:"Panier vide"});let subtotal=c.rows.reduce((s,x)=>s+x.price_cents*x.quantity,0),shipping=Math.round(Number(req.body.shipping||4.99)*100),client=await pool.connect();try{await client.query("BEGIN");let o=(await client.query("INSERT INTO orders(buyer_id,subtotal_cents,shipping_cents,total_cents) VALUES($1,$2,$3,$4) RETURNING *",[req.user.id,subtotal,shipping,subtotal+shipping])).rows[0];for(const x of c.rows)await client.query("INSERT INTO order_items(order_id,product_id,seller_id,title,unit_price_cents,quantity) VALUES($1,$2,$3,$4,$5,$6)",[o.id,x.product_id,x.seller_id,x.title,x.price_cents,x.quantity]);await client.query("DELETE FROM carts WHERE buyer_id=$1",[req.user.id]);await client.query("COMMIT");res.status(201).json(o)}catch(e){await client.query("ROLLBACK");res.status(500).json({error:"Commande impossible"})}finally{client.release()}});
app.get("/api/orders",auth,async(req,res)=>{let sql=req.user.role==="admin"?"SELECT * FROM orders ORDER BY created_at DESC":"SELECT * FROM orders WHERE buyer_id=$1 ORDER BY created_at DESC";let r=await pool.query(sql,req.user.role==="admin"?[]:[req.user.id]);res.json(r.rows)});
app.post("/api/orders/:id/payment",auth,async(req,res)=>{let r=await pool.query("SELECT * FROM orders WHERE id=$1 AND buyer_id=$2",[req.params.id,req.user.id]),o=r.rows[0];if(!o)return res.status(404).json({error:"Commande introuvable"});if(!stripe)return res.status(503).json({error:"Stripe n'est pas configuré. Ajoutez STRIPE_SECRET_KEY."});let intent=await stripe.paymentIntents.create({amount:o.total_cents,currency:"eur",metadata:{order_id:String(o.id)}});await pool.query("UPDATE orders SET stripe_payment_intent=$1 WHERE id=$2",[intent.id,o.id]);res.json({clientSecret:intent.client_secret})});
app.post("/api/webhooks/stripe",express.raw({type:"application/json"}),(req,res)=>{if(!stripe)return res.sendStatus(200);try{let event=stripe.webhooks.constructEvent(req.body,req.headers["stripe-signature"],process.env.STRIPE_WEBHOOK_SECRET);if(event.type==="payment_intent.succeeded"){let id=event.data.object.metadata.order_id;pool.query("UPDATE orders SET payment_status='paid',status='confirmed' WHERE id=$1",[id])}res.json({received:true})}catch(e){res.status(400).send("Webhook error")}});

app.get("/api/favorites",auth,async(req,res)=>{let r=await pool.query("SELECT p.* FROM favorites f JOIN products p ON p.id=f.product_id WHERE f.buyer_id=$1 ORDER BY f.created_at DESC",[req.user.id]);res.json(r.rows)});
app.post("/api/favorites/:id",auth,async(req,res)=>{await pool.query("INSERT INTO favorites(buyer_id,product_id) VALUES($1,$2) ON CONFLICT DO NOTHING",[req.user.id,req.params.id]);res.status(201).json({ok:true})});
app.delete("/api/favorites/:id",auth,async(req,res)=>{await pool.query("DELETE FROM favorites WHERE buyer_id=$1 AND product_id=$2",[req.user.id,req.params.id]);res.json({ok:true})});
app.post("/api/seller/profile",auth,async(req,res)=>{if(!["seller","admin"].includes(req.user.role))return res.status(403).json({error:"Compte vendeur requis"});let b=req.body,r=await pool.query("INSERT INTO seller_profiles(user_id,shop_name,bio,avatar_url,city) VALUES($1,$2,$3,$4,$5) ON CONFLICT(user_id) DO UPDATE SET shop_name=EXCLUDED.shop_name,bio=EXCLUDED.bio,avatar_url=EXCLUDED.avatar_url,city=EXCLUDED.city RETURNING *",[req.user.id,b.shopName,b.bio||"",b.avatarUrl||"",b.city||""]);res.json(r.rows[0])});
app.get("/api/sellers/:id",async(req,res)=>{let r=await pool.query("SELECT sp.*,u.name,u.id user_id FROM seller_profiles sp JOIN users u ON u.id=sp.user_id WHERE sp.user_id=$1",[req.params.id]);if(!r.rows[0])return res.status(404).json({error:"Vendeur introuvable"});let p=await pool.query("SELECT * FROM products WHERE seller_id=$1 AND status='approved' ORDER BY created_at DESC",[req.params.id]);res.json({seller:r.rows[0],products:p.rows})});
app.post("/api/conversations",auth,async(req,res)=>{let b=req.body,prod=(await pool.query("SELECT seller_id FROM products WHERE id=$1",[b.productId])).rows[0];if(!prod)return res.status(404).json({error:"Produit introuvable"});let buyer=req.user.id,seller=prod.seller_id;if(buyer===seller)return res.status(400).json({error:"Conversation invalide"});let r=await pool.query("INSERT INTO conversations(buyer_id,seller_id,product_id) VALUES($1,$2,$3) ON CONFLICT(buyer_id,seller_id,product_id) DO UPDATE SET product_id=EXCLUDED.product_id RETURNING *",[buyer,seller,b.productId]);res.status(201).json(r.rows[0])});
app.get("/api/conversations",auth,async(req,res)=>{let r=await pool.query("SELECT c.*,p.title,ub.name buyer_name,us.name seller_name FROM conversations c JOIN products p ON p.id=c.product_id JOIN users ub ON ub.id=c.buyer_id JOIN users us ON us.id=c.seller_id WHERE c.buyer_id=$1 OR c.seller_id=$1 ORDER BY c.created_at DESC",[req.user.id]);res.json(r.rows)});
app.get("/api/conversations/:id/messages",auth,async(req,res)=>{let ok=await pool.query("SELECT 1 FROM conversations WHERE id=$1 AND (buyer_id=$2 OR seller_id=$2)",[req.params.id,req.user.id]);if(!ok.rows[0])return res.status(403).json({error:"Accès refusé"});let r=await pool.query("SELECT m.*,u.name sender_name FROM messages m JOIN users u ON u.id=m.sender_id WHERE conversation_id=$1 ORDER BY m.created_at",[req.params.id]);res.json(r.rows)});
app.post("/api/conversations/:id/messages",auth,async(req,res)=>{let ok=await pool.query("SELECT * FROM conversations WHERE id=$1 AND (buyer_id=$2 OR seller_id=$2)",[req.params.id,req.user.id]);if(!ok.rows[0])return res.status(403).json({error:"Accès refusé"});if(!req.body.body?.trim())return res.status(400).json({error:"Message vide"});let r=await pool.query("INSERT INTO messages(conversation_id,sender_id,body) VALUES($1,$2,$3) RETURNING *",[req.params.id,req.user.id,req.body.body.trim()]);let other=req.user.id===ok.rows[0].buyer_id?ok.rows[0].seller_id:ok.rows[0].buyer_id;await pool.query("INSERT INTO notifications(user_id,type,title,body) VALUES($1,'message','Nouveau message',$2)",[other,"Vous avez reçu un nouveau message"]);res.status(201).json(r.rows[0])});
app.get("/api/notifications",auth,async(req,res)=>{let r=await pool.query("SELECT * FROM notifications WHERE user_id=$1 ORDER BY created_at DESC LIMIT 50",[req.user.id]);res.json(r.rows)});
app.get("/api/admin/stats",auth,admin,async(req,res)=>{let [u,p,o,r]=await Promise.all([pool.query("SELECT count(*) n FROM users"),pool.query("SELECT count(*) n FROM products"),pool.query("SELECT count(*) n FROM orders"),pool.query("SELECT COALESCE(sum(total_cents),0) n FROM orders WHERE payment_status='paid'")]);res.json({users:+u.rows[0].n,products:+p.rows[0].n,orders:+o.rows[0].n,revenue_cents:+r.rows[0].n})});
app.patch("/api/admin/orders/:id",auth,admin,async(req,res)=>{let r=await pool.query("UPDATE orders SET shipping_status=COALESCE($1,shipping_status),status=COALESCE($2,status) WHERE id=$3 RETURNING *",[req.body.shippingStatus,req.body.status,req.params.id]);res.json(r.rows[0])});
app.patch("/api/admin/products/:id",auth,admin,async(req,res)=>{let r=await pool.query("UPDATE products SET status=$1 WHERE id=$2 RETURNING *",[req.body.status,req.params.id]);res.json(r.rows[0])});
app.listen(process.env.PORT||3000,()=>console.log("Secondeo Market V7 prêt"));
